import { Meta, Story, ArgsTable, Canvas, Preview } from '@storybook/addon-docs';

import * as AlertComponents from './alert.stories.ts';

import {AlertComponent} from './alert.component';

<Meta
  title="Component/AlertComponent/AlertComponentDocs"
  component={AlertComponent}
/>

# Alert Component

---

The alert method displays an alert with a message .

Alerts are available for any length of text, as well as an optional dismiss button.

The alert method is used when you want information to come through to the user.

We can import the alert using the following importation :

```
    import { AlertComponent } from ' @ptg-ui/angular'
```

Types of controls supported by button:

- message
- isDismissible

## Quick Start

we can create a alert using the following HTML Tag:

```
<ptg-ui-alert message="Hello" [isDismissible]="true"></ptg-ui-alert>

```

Following are some controls with their explanation :

1] message :

Alert messages can be used to notify the user about something special.

<Canvas><Story id="component-alertcomponent--message" /></Canvas>


2]isDismissible :

The isDismissible attribute specifies whether the alert  will be dismissed when user taps on the scrim.

<Canvas><Story id="component-alertcomponent--is-dismissible" /></Canvas>