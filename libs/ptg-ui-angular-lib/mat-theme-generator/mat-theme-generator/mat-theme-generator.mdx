import { Meta, Story, ArgsTable, Canvas , Preview} from '@storybook/addon-docs';
import { MatThemeGeneratorComponent } from './mat-theme-generator.component';
import { ReactiveFormsModule } from '@angular/forms';
import { MatDialogModule } from '@angular/material/dialog';
import * as MatThemeGeneratorComponents from './mat-theme-generator.stories.ts';

<Meta title="Components/MatThemeGenerator/MatThemeGeneratorDocs" component={MatThemeGeneratorComponent} />

# MatThemeGenerator Component

This component allows users to generate and save theme colors.

We can import the Theme tag using the following importation :

```
    import { MatThemeGeneratorModule } from '@ptg-ui/angular/mat-theme-generator'
```
## Quick Start

we can create a theme-generator using the following HTML Tag:

```
  <ptg-ui-mat-theme-generator></ptg-ui-mat-theme-generator>`;


```

To view demo of theme-generator :

<Canvas>
  <Story id="component-matthemegeneratorcomponent--primary" />
</Canvas>
