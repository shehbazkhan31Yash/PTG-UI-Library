import { Meta, Story, ArgsTable, Canvas, Preview } from '@storybook/addon-docs';


import { CarouselComponent } from './carousel.component';
import * as CarouselStories from './carousel.stories';

<Meta of={CarouselStories} />



# Carousel Component

---

The carousel is a slideshow for cycling through a series of content, built with CSS 3D transforms and a bit of JavaScript. It works with a series of images, text, or custom markup. It also includes support for previous/next controls and indicators.

The .active class needs to be added to one of the slides otherwise the carousel will not be visible. Also be sure to set a unique id on the .carousel for optional controls, especially if you’re using multiple carousels on a single page.

We can import the carousel tag using the following importation :

```
    import { CarouselModule } from '@ptg-ui/angular/carousel'
```


## Quick Start

we can create a carousel using the following HTML Tag:

``` 
<ptg-ui-carousels [carouselItems]="sampleImageWithControls" [showControls]="true"></ptg-ui-carousels>`;

```

Following are some controls with their explanation :

1] Slide only:

Here’s a carousel with slides only.

<Canvas of={carouselStories.Primary} />


2] With Controls :

Adding in the previous and next controls.

<Canvas of={carouselStories.ShowControls} />

3] With indicators :

You can also add the indicators to the carousel, alongside the controls, too.

<Canvas of={carouselStories.ShowIndicators} />

4] With Caption :

You can also add the Caption to the carousel.

<Canvas of={carouselStories.ShowContent} />

